// async function run() {
//   try {
//     // Connect the client to the server	(optional starting in v4.7)
//     await client.connect();
//     // Send a ping to confirm a successful connection
//     await client.db("admin").command({ ping: 1 });
//     console.log("Pinged your deployment. You successfully connected to MongoDB!");




// const newcoffee = client.db('coffeedb').collection('coffee');

// app.get('/coffee', async (req, res) => { 

// //  const cursor = newcoffee.find();
// //  const result =await cursor.toArray();
// //OR
//  const result = await newcoffee.find().toArray();
//  res.send(result);

// }); 


// app.get('/coffee/:id', async (req, res) => {
//       const id = req.params.id;
//       const query = { _id: new ObjectId(id) };
//       const user = await newcoffee.findOne(query);
//       res.send(user);
//     });

// app.post('/coffee', async(req,res)=>{ 
//     console.log('data' , req.body ) 
//     const newuser = req.body; 
//      const result = await newcoffee.insertOne(newuser)
//     // newuser.id=user.length + 1; 
//     res.send(result) 

// }) 


// //update
//  app.put('/coffee/:id', async (req, res) => {
//       const id = req.params.id;
//       const filter = { _id: new ObjectId(id) };
//       const updatedCoffee =req.body;
//       const updatedDoc = {
//         $set: updatedCoffee
         
        
//       };
//       const options = { upsert: true };
//       const result = await newcoffee.updateOne(filter, updatedDoc, options);
//       res.send(result);
//     });

// app.delete('/coffee/:id', async(req, res)=>{
//     const id = req.params.id;
//     console.log("to be deleted",id);
//     const query = {_id : new ObjectId(id)} ;
//     const result = await newcoffee.deleteOne(query);
//     res.send(result);
// })

//   } finally {
//     // Ensures that the client will close when you finish/error
//     // await client.close();
//   }
// }
// run().catch(console.dir);